# 開発ルール

- 実装前に`docs/`内の関連ファイルを確認し、その内容を要求仕様として実装を進める。
- 作業内容がまとまった適切な単位でコミットする。
- Phase完了時など区切りのよい時点で、検証結果と作業ツリーの状態を確認して作業ブランチをリモートへpushする。
- コミットメッセージは`prefix: 日本語メッセージ`形式にする。
  - 例: `feat: コメント機能を追加`
- オーバーエンジニアリングを避け、要件を満たす最小で単純な実装を優先する。
- 依存関係がなく競合しない作業は、ブランチやworktree等で分離し、可能な限り並列で進める。統合前に各差分を確認し、統合後に全体の検証を行う。
- 作業ごとに、実装内容を簡潔に説明する。
- コード変更後は`npm run format`でPrettierを適用し、`npm run check`でフォーマットを含む全検証を行う。
- 人間による操作が必要な場合は、必要な理由と実行手順を明確に示す。

## Issue・ブランチ・PR

- 実装作業はGitHub Issueを起点にする。依頼に対応するIssueがなければ、背景、期待する結果、検証可能な完了条件、対象外を整理したIssueを作成してから着手する。
- 実装方法を変えるほど重要な不明点は着手前に確認する。小さな実装判断はPRの「実装時に判断したこと」へ記録する。
- Issueの完了条件に含まれない変更は現在のPRへ混ぜず、必要であれば別Issueとして提案する。
- 作業ブランチは最新の`main`から作り、`<type>/issue-<番号>-<短い説明>`形式で命名する。例: `feat/issue-42-unread-marker`、`fix/issue-51-event-card`、`chore/issue-60-ci`。
- 原則として1 Issueにつき1ブランチ・1 PRとする。複数Issueをまとめる必要がある場合は、依存関係とまとめる理由をPRへ明記する。
- `main`へ直接pushしない。変更は必ず作業ブランチからPRを経由して反映する。
- PR本文は`.github/pull_request_template.md`に従い、`Closes #<Issue番号>`、変更内容、判断事項、対象外、検証結果、画面変更、バージョン判断、リスクを記載する。
- UI変更では、家族の写真などの個人情報を含まないスクリーンショットまたは具体的な画面確認結果をPRへ記載する。
- PR作成後はGitHub Actionsの`npm run check`が成功するまで確認する。失敗した場合はjob logを確認し、同じ作業ブランチで修正する。
- AIは、ユーザーから明示的にマージを指示されるまでPRをマージしない。
- PRがマージされ、必要なCI・デプロイ・本番確認が完了したら、GitHubの自動削除によって作業ブランチが削除済みであることを確認する。残っている場合だけ手動で削除する。未マージのPR、調査・復旧で引き続き必要なブランチ、`main`などの保護対象ブランチは削除しない。

## バージョン管理

- アプリのバージョンは`package.json`を正とし、`MAJOR.MINOR.PATCH`形式で管理する。変更時は`package-lock.json`も同期する。
- バージョンはコミットごとではなく、本番へ反映する一連の変更を1リリースとして、その内容に応じて1回だけ上げる。
- `0.x`の開発段階では次の基準を使う。
  - `PATCH`: 不具合修正、表示崩れの修正、文言や配色などの小さなUI改善、既存動作を変えない保守対応。
  - `MINOR`: 新しい画面・機能・操作の追加、既存機能の大幅な変更、API・権限・データ構造などの互換性を壊す変更。
  - `1.0.0`: 主要機能と運用が安定し、以後の互換性を維持する段階へ移ると判断したとき。
- `1.0.0`以降はSemantic Versioningに従い、後方互換性のある機能追加は`MINOR`、後方互換性のある修正は`PATCH`、互換性を壊す変更は`MAJOR`を上げる。
- 複数種類の変更を同時に公開する場合は、最も大きい変更区分を採用する。
- ドキュメント、テスト、コメント、リファクタリング、CIなど、配信されるアプリの機能や挙動が変わらない変更だけの場合はバージョンを上げない。
- バージョンを上げる場合は、検証・コミット・デプロイより前に行い、実装変更と同じリリースへ含める。Git tagやGitHub Releaseは、別途ユーザーから指示がある場合だけ作成する。

## 本番反映

- `main`へのpushを契機に`.github/workflows/deploy.yml`がD1 migration、Workerのデプロイ、配信状態とHTTP 200の確認まで自動実行する。`main`へのpushはPRのマージでのみ発生させる。通常はローカルからmigration適用や`npm run deploy`を実行しない。
- ユーザーがデプロイ不要と明示した場合を除き、実装から本番反映までは次の固定手順で進める。コード・設定・配信物が変わらないドキュメントのみの変更は、本番確認を不要とする。
  1. 作業ブランチで`npm run check`、`git diff --check`、`git status --short`を実行し、対象変更だけであることを確認する。
  2. 対象変更をコミットして作業ブランチへpushし、Issueに紐づくPRを作成する。
  3. PRのGitHub Actionsが成功するまで確認する。失敗した場合はjob logを確認し、原因を修正して同じPRへpushする。
  4. 人間が内容を確認し、ユーザーが明示的に指示した後でPRを`main`へマージする。
  5. マージコミットの`.github/workflows/deploy.yml`が成功するまで確認し、変更内容に応じた最小限の本番確認を行う。
  6. 必要な確認が完了したら、GitHubの自動削除によってマージ済みPRの作業ブランチが残っていないことを確認する。残っている場合だけ手動で削除する。
- Cloudflare・Wranglerの追加調査や手動デプロイは、`wrangler.jsonc`、binding、migration、package script、デプロイ方式を変更するとき、自動デプロイが失敗したとき、または上位指示で必要なときだけ行う。
- 作業ツリーに別作業の未コミット差分がある場合は混ぜず、対象変更用の一時worktreeで検証・コミット・pushする。
