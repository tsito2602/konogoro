# 開発ルール

## 完了条件と進め方

- 依頼された変更は、必要な検証、Issueに紐づくPR、最新commitの必須CI、stagingデプロイの成功確認まで進める。通常の実装判断や承認済みの工程ごとに確認を求めない。本番へのマージは別途ユーザーの明示指示を必要とする。
- ユーザーから対象作業で明示的に依頼された場合だけサブエージェントを使う。通常は調査・実装・レビュー・検証をメインエージェントで行う。独立した読み取り・検索はまとめてよい。
- 依頼とIssueの完了条件に沿って実装し、仕様を変更した場合は関連文書も更新する。途中の質問・追加要望を反映し、明示的な中止・目的変更がなければ作業を継続する。
- 実装方針を変える不明点だけを確認し、回答に依存しない作業は先に進める。既存の未コミット差分は混ぜず、必要ならworktreeを分ける。

## 作業に応じた参照先

関係する節だけを参照する。資料の役割が不明な場合は[文書一覧](docs/README.md)を使う。

| 作業                      | 参照先                                                                         |
| ------------------------- | ------------------------------------------------------------------------------ |
| 機能範囲・権限            | [機能要件](docs/01_REQUIREMENTS.md)                                            |
| 構成・サービス境界        | [設計](docs/02_ARCHITECTURE.md)                                                |
| スキーマ・保存データ      | [データモデル](docs/03_DATA_MODEL.md)                                          |
| 画面・操作・共通デザイン  | [UI・UX仕様](docs/04_UI_UX_SPEC.md)、[デザインガイド](docs/05_DESIGN_GUIDE.md) |
| PRのstaging反映・失敗調査 | [konogoro-staging skill](.agents/skills/konogoro-staging/SKILL.md)             |
| 本番反映・運用変更        | [運用](docs/08_OPERATIONS.md)                                                  |

レイアウト変更では対応するSkeletonも同じPRで更新し、配置・権限判定を実画面と共有する。未知のデータを捏造せず、追加読込中は既存内容を残す。

## 差分に応じた検証

| 変更                         | 必要な確認                                                                                   |
| ---------------------------- | -------------------------------------------------------------------------------------------- |
| 文書・コメントのみ           | 変更ファイルの整形、リンク・仕様との整合、差分。ローカルの全アプリ検証・実機・本番確認は不要 |
| コード・設定                 | 変更ファイルを整形し、最終差分で `npm run check`。内包するlint・build等を別途重複実行しない  |
| UI                           | 変更した画面・状態と、影響するスマホ／PC・権限の代表ケース                                   |
| 認証・権限・保存・データ移行 | 許可／拒否、成功／失敗、データ保持など、変更に関係する境界と回帰                             |

- 整形は変更ファイルに限定する。ローカルgit作業では `git diff --check` と `git status --short` で差分の混入を確認する。PRの必須CIは変更種別によらず維持する。
- 同じcommit・環境の成功結果は再利用し、修正・失敗・環境差・具体的な未解決リスクがある場合だけ関連確認を繰り返す。CIに必要な再実行は維持する。
- 新しいロジックや具体的な回帰に対してテストを追加する。単純な文言・色・余白の変更に実装を写したテストは追加しない。共通レイアウト・権限の変更は影響する範囲まで確認する。
- 完了条件と必要な確認を満たしたらレビューを終え、結果・根拠・判断に関わる制約だけを短く報告する。

## Issue・ブランチ・PR

- 原則1 Issueにつき1ブランチ・1 PR。Issueがなければ背景・期待する結果・検証可能な完了条件を整理して作成する。別目的の変更は混ぜず、依存するIssueをまとめる場合は理由をPRへ記載する。
- 作業ブランチは最新の `main` から `<type>/issue-<番号>-<短い説明>` で作る。`main` へ直接pushしない。`staging` は統合確認専用で、作業ブランチの派生元やmain向けPRのheadにしない。
- コミットは意味のある単位でまとめ、メッセージは `prefix: 日本語メッセージ` とする。検証済み差分をpushし、[PRテンプレート](.github/pull_request_template.md)に沿って関連Issue・目的と変更・検証結果を記載する。
- UI変更では、個人情報を含まない画像・録画または具体的な画面確認結果をPRへ添える。特別な判断・リスク・バージョンの説明は該当時だけ記載する。
- GitHub操作は利用可能な認証済み接続を優先する。GitHub接続とローカルgitの認証状態を区別し、検証済み差分だけを反映する。GitHub接続で登録した場合はローカルとリモートのtreeの一致を確認する。
- 認証・権限・保護ルール・自動承認レビューにより拒否された場合は迂回しない。原因と未完了の操作を具体的に示し、必要な追加許可・根拠が得られた範囲で再開する。
- PRの作成・更新後は、文書のみ・Draftを含めて[staging skill](.agents/skills/konogoro-staging/SKILL.md)に従い反映する。目視確認待ちやローカルブラウザ不調は省略の理由にしない。

## バージョンと本番反映

- バージョンは `package.json` を正として `package-lock.json` と同期する。1リリースにつき1回、修正はPATCH、後方互換の機能追加はMINOR、互換性を壊す変更はMAJORとし、最も大きい区分を採用する。配信する挙動が変わらない文書・テスト・CI等のみの変更では上げない。
- バージョン変更は検証・コミットより前に含める。Git tag・GitHub Releaseは別途指示がある場合だけ作成する。
- ユーザーの明示指示後に元のPRを `main` へマージし、`.github/workflows/deploy.yml` の成功と変更に応じた本番確認まで行う。通常はローカルから本番migrationや `npm run deploy` を実行しない。
- Cloudflare・Wranglerの追加調査や手動操作は、構成・binding・migration・デプロイ方式の変更、自動デプロイ失敗、または依頼で必要な場合に行う。
- 本番確認後、不要になったマージ済み作業ブランチが残っている場合だけ削除する。未マージ・確認中・復旧に必要なブランチと保護対象ブランチは保持する。
