# 開発ルール

- 実装前に`docs/`内の関連ファイルを確認し、その内容を要求仕様として実装を進める。
- 作業内容がまとまった適切な単位でコミットする。
- Phase完了時など区切りのよい時点で、検証結果と作業ツリーの状態を確認して作業ブランチをリモートへpushする。
- コミットメッセージは`prefix: 日本語メッセージ`形式にする。
  - 例: `feat: コメント機能を追加`
- オーバーエンジニアリングを避け、要件を満たす最小で単純な実装を優先する。
- モデルを問わずサブエージェントを使用しない。調査・実装・レビュー・検証はメインエージェントだけで行う。独立した読み取り・検索のツール呼び出しはまとめてよい。
- 作業ごとに、実装内容を簡潔に説明する。
- レイアウト変更時は対応するSkeletonも同じPRで更新し、実画面と配置・権限判定を共有する。未知のデータを捏造せず、追加読込では既存内容を残す。検証範囲は下記に従う。
- 整形・検証・レビューは「差分に応じた検証」に従い、同じ確認を工程ごとに繰り返さない。
- 人間による操作が必要な場合は、必要な理由と実行手順を明確に示す。

## 差分に応じた検証

レビューは「Issueの目的を満たすか」「差分が具体的な不具合を生まないか」「必要な検証証拠があるか」の3点を基本とする。変更ファイルと直接影響する呼び出し元・仕様から確認し、無関係な全画面・全資料を読み直さない。

| 変更                         | 必要な確認                                                                                                           |
| ---------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| 文書・コメントのみ           | 変更ファイルの整形、リンク・仕様の整合、差分。ローカルの全アプリ検証・実機・デプロイ確認は不要                       |
| コード・設定                 | 整形後、最終差分で `npm run check` を1回。既存スクリプトに含まれるlint・build等を別途重複実行しない                  |
| UI                           | 変更した画面・状態と、影響するスマホ／PC・権限の代表ケース。共通レイアウトや権限表示を変えた場合だけ関連範囲を広げる |
| 認証・権限・保存・データ移行 | 関連する許可／拒否、成功／失敗、データ保持など具体的な境界・回帰リスク                                               |

変更ファイルにPrettierを適用し、ローカルgit作業では `git diff --check` と `git status --short` で混入を確認する。小変更のために全リポジトリへ整形差分を作らない。PRの必須CIは変更種別によらず維持する。

- 同じcommit・同じ環境の成功結果は再利用する。修正・失敗・環境差・具体的な未解決リスクがある場合だけ関連確認を繰り返す。CIが必要とする再実行は妨げない。
- 単純な文言・色・余白の変更に実装を写したテストを追加しない。新しいロジックや具体的な回帰には必要なテストを追加する。
- 各文書の確認観点は影響範囲を選ぶために使い、すべてを毎回のチェックリストにしない。未実施事項は判断を変える制約だけ記載する。
- 報告は結果と根拠を短くまとめる。成功ログ全文、問題のない箇所の列挙、同じ結果の転記を避ける。必要な確認が通ればレビューを終える。

## Issue・ブランチ・PR

- 実装作業はGitHub Issueを起点にする。依頼に対応するIssueがなければ、背景、期待する結果、検証可能な完了条件、対象外を整理したIssueを作成してから着手する。
- 実装方法を変えるほど重要な不明点は着手前に確認する。小さな実装判断はPRの「実装時に判断したこと」へ記録する。
- Issueの完了条件に含まれない変更は現在のPRへ混ぜず、必要であれば別Issueとして提案する。
- 作業ブランチは最新の`main`から作り、`<type>/issue-<番号>-<短い説明>`形式で命名する。例: `feat/issue-42-unread-marker`、`fix/issue-51-event-card`、`chore/issue-60-ci`。
- 原則として1 Issueにつき1ブランチ・1 PRとする。複数Issueをまとめる必要がある場合は、依存関係とまとめる理由をPRへ明記する。
- `main`へ直接pushしない。変更は必ず作業ブランチからPRを経由して反映する。
- PR本文はテンプレートに沿って、関連Issue・目的と変更・検証結果を簡潔に記載する。特別な判断、画面資料、リスク、バージョンは該当時だけ添え、同じ説明を複数欄へ転記しない。
- UI変更では、家族の写真などの個人情報を含まないスクリーンショットまたは具体的な画面確認結果をPRへ記載する。
- ユーザーからIssueの実装を指示された場合、その指示には対象作業ブランチのリモートへのpush、PR作成、GitHub Actionsの確認までを含むものとし、これらの実行について追加確認を求めない。ただし、実際の権限不足、認証失敗、保護ルールによる拒否が発生した場合は迂回せず停止して報告する。
- AIは、ユーザーから明示的にマージを指示されるまでPRをマージしない。
- GitHub操作は、対象リポジトリへの認証済みGitHub接続（アプリ／MCP）が利用可能なら優先する。ローカルの`gh`や`git push`の認証状態と、GitHub接続の認証状態を混同しない。
- GitHub接続で変更を反映する場合は、対象リポジトリと最新のbase・作業ブランチを確認し、検証済み差分だけをblob・tree・commitとして登録する。作業ブランチへの反映、PR作成、最新commitのCI確認まで続け、ローカルとリモートのtreeが一致することを確認する。
- 同じ対象・差分・操作についてユーザーから得た許可は会話内で引き継ぎ、手順上の都合だけで同じ許可や特定の定型文を繰り返し求めない。実装許可とマージ許可は区別する。
- 実際の権限不足、保護ルール、自動承認レビューによる拒否は別ツールで迂回しない。拒否理由と既存の許可を確認し、必要な追加許可・根拠が得られた場合に限って、その範囲で再開する。停止が必要な場合は、認証不足・権限不足・自動審査拒否のどれかと、未完了の操作を具体的に伝える。
- PRがマージされ、必要なCI・デプロイ・本番確認が完了したら、GitHubの自動削除によって作業ブランチが削除済みであることを確認する。残っている場合だけ手動で削除する。未マージのPR、調査・復旧で引き続き必要なブランチ、`main`などの保護対象ブランチは削除しない。

## バージョン管理

- アプリのバージョンは`package.json`を正とし、`MAJOR.MINOR.PATCH`形式で管理する。変更時は`package-lock.json`も同期する。
- バージョンはコミットごとではなく、本番へ反映する一連の変更を1リリースとして、その内容に応じて1回だけ上げる。
- `0.x`の開発段階では次の基準を使う。
  - `PATCH`: 不具合修正、表示崩れの修正、文言や配色などの小さなUI改善、既存動作を変えない保守対応。
  - `MINOR`: 新しい画面・機能・操作の追加、既存機能の大幅な変更、API・権限・データ構造などの互換性を壊す変更。
  - `1.0.0`: 主要機能と運用が安定し、以後の互換性を維持する段階へ移ると判断したとき。
- `1.0.0`以降はSemantic Versioningに従い、後方互換性のある機能追加は`MINOR`、後方互換性のある修正は`PATCH`、互換性を壊す変更は`MAJOR`を上げる。
- 複数種類の変更を同時に公開する場合は、最も大きい変更区分を採用する。
- ドキュメント、テスト、コメント、リファクタリング、CIなど、配信されるアプリの機能や挙動が変わらない変更だけの場合はバージョンを上げない。
- バージョンを上げる場合は、検証・コミット・デプロイより前に行い、実装変更と同じリリースへ含める。Git tagやGitHub Releaseは、別途ユーザーから指示がある場合だけ作成する。

## 本番反映

- `main`へのpushを契機に`.github/workflows/deploy.yml`がD1 migration、Workerのデプロイ、配信状態とHTTP 200の確認まで自動実行する。`main`へのpushはPRのマージでのみ発生させる。通常はローカルからmigration適用や`npm run deploy`を実行しない。
- ユーザーがデプロイ不要と明示した場合を除き、実装から本番反映までは次の固定手順で進める。コード・設定・配信物が変わらないドキュメントのみの変更は、本番確認を不要とする。
  1. 「差分に応じた検証」を一度完了し、対象差分だけであることを確認する。
  2. 対象変更をコミットして作業ブランチへpushし、Issueに紐づくPRを作成する。
  3. PRの最新commitに対する必須CI（`npm run check`）の成功を確認する。失敗時だけ該当jobのログを調べ、同じPRで修正する。
  4. 人間が内容を確認し、ユーザーが明示的に指示した後でPRを`main`へマージする。
  5. マージコミットの`.github/workflows/deploy.yml`が成功するまで確認し、変更内容に応じた最小限の本番確認を行う。
  6. 必要な確認が完了したら、GitHubの自動削除によってマージ済みPRの作業ブランチが残っていないことを確認する。残っている場合だけ手動で削除する。
- Cloudflare・Wranglerの追加調査や手動デプロイは、`wrangler.jsonc`、binding、migration、package script、デプロイ方式を変更するとき、自動デプロイが失敗したとき、または上位指示で必要なときだけ行う。
- 作業ツリーに別作業の未コミット差分がある場合は混ぜず、対象変更用の一時worktreeで検証・コミット・pushする。

## 検証環境

- 複数PRをまとめて実機確認するときは、`docs/09_STAGING.md`に従って常設の`staging`ブランチへ対象ブランチを統合し、staging専用Cloudflare環境へデプロイする。
- ユーザーへ実機確認を依頼する前に、対象PRを`staging`へ統合し、`Deploy staging to Cloudflare`の成功とstaging画面に表示されるバージョンが対象リリースと一致することを確認する。対象変更が未反映の状態では実機確認を依頼しない。
- `staging`は統合確認専用とし、作業ブランチの派生元や`main`へのPRのheadには使わない。各作業ブランチは最新の`main`から作成し、元のPRを個別に`main`へマージする。
- staging確認前は`staging`を最新の`main`と同じcommitへ更新し、対象ブランチを依存関係の順に統合する。本番反映後は再び最新の`main`と同じcommitへ戻す。
- stagingには本番データを複製せず、個人情報を含む写真・動画・コメントを登録しない。stagingからLINE通知を送信しない。
