# このごろ — UI・UX仕様

画面の情報・操作・状態・遷移を定義する。視覚の数値は[デザインガイド](05_DESIGN_GUIDE.md)を参照する。

2026-09改訂: 「目標仕様 #番号」は後続Issueで導入する要求であり、現行実装済みではない。それ以外の既存機能は維持する。各Issueは[改訂記録](11_DESIGN_REVISION.md)と本書を起点に差分だけを実装する。

2026-09-07の表現改訂（#111）は末尾の「全画面の質感・動きの目標仕様」に定義する。そこに示す表現は後続実装の目標であり、既存の機能・権限・状態遷移と区別する。

## 共通

モバイルファースト。

主対象:

- 320px〜430px

PCでも使用可能だが、スマホUIを無制限に横へ引き伸ばさない。

PC幅ではBottom Navigationを左側の常設ナビゲーションへ切り替え、メインコンテンツと操作領域を分離する。ownerとuploaderには左ナビ上部へ、通常のナビ項目と区切った目立つ追加枠を常時表示する。1024px以上でPC表示へ切り替え、本文は最大1280pxのキャンバス内へ配置し、画面タイトルと操作も同じ左右位置へ揃える。1024pxから1199pxでは無理に多列化せず広い1列または2列を使い、1200px以上では画面の用途に応じて補助領域を分離する。モバイルとタブレット幅では従来どおりBottom Navigationと1列の画面構成を使う。

PC表示では、Timelineは読みやすい幅の投稿列と未閲覧導線の補助列、Activityは最終閲覧者と履歴、Event Detailはカバー・概要と投稿、Settingsと管理画面は関連する設定群をそれぞれ別の列へ配置する。Event Listは3列、Albumの月表示は代表Mediaと一覧を分け、年表示は6列を上限とする。Post DetailはMediaと投稿情報・コメントを左右へ分ける。投稿作成画面は写真・動画の選択・確認領域と投稿内容の入力領域を2列にし、広い画面で操作が散らばらない情報密度にする。

固定表示するヘッダー、保存操作、トースト、Post Detailは左ナビゲーションと重ならず、PC側のメインキャンバスを基準に配置する。Skeletonも各PC画面の最終的なカラム構成と幅に合わせる。

画面ヘッダーは原則:

- 画面タイトル
- 必要なアクション

のみ。

説明サブタイトルは不要。画面タイトルはスマホ・PCとも1行に保ち、戻るボタンと権限ごとの操作領域を確保した残り幅を超える場合は末尾を省略記号で表示する。自動スクロールは行わず、見出しのテキストとtitle属性には全文を保持する。読み込み中も共通のPageHeaderを使い、同じ1行の高さと配置に揃える。

操作成功のうち、画面に残す必要がなく、遷移後には結果だけを短く伝えるものはトーストで表示する。対象は設定保存、投稿とイベントの作成・編集・削除、招待URLのコピーとする。入力エラー、読み込み失敗、削除確認はトーストにせず、操作箇所の近くまたはモーダルに表示する。コメントや権限変更のように結果がその場で明確に反映される独立操作にはトーストを表示しない。

データ取得中は、各画面の完成後の構造に近いSkeletonを表示する。画像やテキストの大きさを読み込み前から確保してLayout Shiftを抑え、共通の色とshimmer表現を使う。Skeletonの装飾要素は`aria-hidden="true"`とし、読み込み領域には`aria-busy="true"`と支援技術向けの読み込み中表示を設ける。`prefers-reduced-motion: reduce`ではshimmer animationを停止する。

## 初回利用案内

初めてログインしたviewerには、画面全体を使う6ページのガイドを表示する。家族だけで共有する場所であること、タイムライン、イベント、アルバム、コメントとお知らせ、LINE通知を、個人写真を含まない実画面に沿ったイメージと短い説明で案内する。投稿・管理機能は案内せず、設定から開いた場合も閲覧者向けの同じ内容を表示する。横スワイプ、前へ・次へボタン、左右矢印キーでページを移動でき、現在位置を真円のインジケーターで示す。右上の閉じるボタンは全ページで常に表示し、Escapeキーでも閉じられる。見出しは読点の直後で明示的に改行し、単語の途中では改行しない。ガイドを閉じても現在の目的地を変えない。表示済み状態は端末のlocal storageへ保存する。

viewerの利用中にホーム画面への追加案内を自動表示しない。設定の「使い方」から、初回ガイドと端末別のホーム画面追加手順を利用者が選んで再表示できる。local storageを利用できない場合も、各案内を閉じて主要操作を続けられることを優先する。

LINE Login開始時には、pathname、query、hashを同一origin内の戻り先として渡す。初期認証だけでなく、利用中にセッション切れを検知したエラー画面からの再ログインでも同じ戻り先を維持する。

## Bottom Navigation

```text
タイムライン
お知らせ
イベント
アルバム
設定
```

「＋追加」はBottom Navigationから独立させ、そのすぐ上の画面右下へ固定表示する。ownerとuploaderが押すと「写真・動画」と「イベント」を選択できる。viewerには表示しない。

Bottom Navigationのタブを押して画面を切り替えたときは、遷移先を画面先頭から表示する。ほかのタブのスクロール位置は引き継がない。

各項目の役割は次の通りとする。全roleで同じ項目名と並び順を使い、owner/uploaderの投稿・管理操作は従来どおり共通の追加ボタンと各詳細画面から提供する。

- タイムライン: 未閲覧の思い出への入口と、全投稿の一覧
- お知らせ: 新しい投稿やコメントの時系列の履歴
- イベント: 旅行やお出かけ単位でまとめた思い出
- アルバム: 写真・動画をイベント期間の年月から探す一覧
- 設定: 表示、通知、プロフィールと権限に応じた家族管理

「新着」は未閲覧投稿を指すように見え、タイムラインの「新しい思い出を見る」と目的が重複するため、Activityの表示名には「お知らせ」を使う。URLの`/activity`は既存の深いリンクとの互換性のため維持する。

実利用開始後は、viewerが未閲覧投稿を見る際にタイムラインではなく「お知らせ」を誤って選ぶか、「お知らせ」を写真一覧だと認識するかを確認する。5人以上のviewerへタスク確認を行い、2人以上が同じ箇所で迷った場合は、項目数の削減または名称の再検討をIssue化する。

---

# Activity

画面名は「お知らせ」とする。公開投稿とコメントを新しい順に表示する。誰が、どの投稿に、いつ操作したかを表示し、タップするとPost Detailを開く。投稿を表す名称はCaptionではなく見出しを優先し、見出しがない場合はEvent title、どちらもない場合は「投稿」とする。ownerには、リスト上部へ「最後に見た時間」として、メンバーのアイコン、表示名、最終閲覧からの相対時間を横並びで表示する。uploaderとviewerには最終閲覧時間を表示せず、APIからも返さない。閲覧行為はお知らせへ表示しない。お知らせは40件単位で取得し、末尾へ近づいたときに次の項目を自動で追加する。追加取得に失敗した場合は自動再試行を止め、既存の項目を残したまま再試行操作を表示する。

---

# Timeline

タイトル:

```text
タイムライン
```

Postは複数Mediaのまとまり。

イベント開始日を基準に、最初のPostと月が変わる位置へ`2026年9月`形式の年月見出しを表示する。開始日がないEventでは終了日を使う。Eventに属さないPostでは撮影日時、投稿日を順に使い、いずれもない場合は「日付なし」とする。年月見出しは次の年月見出しが来るまで画面ヘッダー直下へ固定し、同じ月のPostが続く間は繰り返さない。

タイムラインは落ち着いた背景の上へPost Cardを1列で並べ、カード間に余白を設ける。各Post Cardは余白と面でひとまとまりに見せ、必要な箇所のみ細い境界線を使う。Media Gridはカード幅いっぱいに表示する。左右の余白は写真の大きさを損なわない範囲に抑える。

現在のユーザーがまだ見ていないPostは、Media Grid上へ「未閲覧」バッジを表示する。Post Cardが自動既読になったらバッジを消す。

viewerのタイムラインでは、未閲覧Postがある場合に限り、上部へ合計件数と「新しい思い出を見る」ボタンを表示する。ボタンから新着閲覧フローへ移動する。

## 新着閲覧フロー

現行実装はPost Cardの50%以上を2秒表示すると自動既読を記録し、成功後に次へ進める。`UnreadPostsPage`には既に残り件数と失敗時の再試行がある。

**目標仕様 #46:** 既読は「投稿が画面に表示された」という受信確認であり、全写真の閲覧や動画の完走を保証しない。この意味を「見た人」と完了文言で一貫させる。メディア単位の既読や全件閲覧の強制は導入しない。

- 未閲覧投稿は公開日時の古い順。件数はサーバーの`view_histories`を正とする。
- 自動既読は2秒の連続表示を基本として維持する。画面より背の高いカードでも到達できる判定領域を定め、50%が永遠に見えず次へ進めない状態を防ぐ。非表示タブ・画面外の時間は数えない。
- 次へは2秒待ちを操作条件にせず、押した時点で現在投稿の既読を記録してから次を取得する。自動判定と重なっても重複処理しない。
- 通常、記録中、次を取得中、失敗を区別する。失敗時は現在の投稿を残し、次へ進んだように見せない。再試行は失敗した段階から行う。
- 残り件数は現在投稿を含む未閲覧数。自動または明示操作の記録成功時に減る。別端末の変更は次のサーバー取得で整合させる。
- 最後の記録と次の取得が成功し未閲覧0件なら「新しい投稿はここまでです」と表示し、タイムラインへ戻れる。全メディアを見たとは表現しない。
- ブラウザバック・途中離脱後はサーバー上の未閲覧から再開する。ローカル保存が使えなくても閲覧を止めない。

## 投稿カード

Media Gridの上には、小さなプロフィールアイコンと投稿者名を横並びにし、その下の独立した行へEvent titleと見出しを表示する。投稿者アイコンは投稿者名の文字と同程度の大きさに抑え、Event titleと見出しにはアクセントカラーを使わず、投稿者と投稿の分類を混同させない。TimelineではEvent titleと見出しを`Event title ＞ 見出し`の順に表示し、Event titleにはカレンダーアイコンを付ける。Eventと見出しのどちらもないPostでは分類の代替文言を表示しない。

Media Gridの下には「見た人」の両目アイコンと人数、コメントアイコン付きのComment導線を並べ、右側へ日付を表示する。狭い幅・文字拡大では補助情報行を折り返してよい。Comment導線は0件なら「コメントを書く」、1件以上なら「コメントN件」と表示する。撮影日時がある場合はカメラアイコンと撮影日、ない場合はアップロードアイコンと投稿日を表示し、読み上げでも日付の種類を区別する。Captionは入力項目名を毎回表示せず本文として読めるようにする。**目標仕様 #44:** 写真の直後にCaption、その次にコメント導線と日付・見た人を置き、写真と言葉の連続性を優先する。Commentがある場合は最新の1件をPost Cardに表示する。2件以上ある場合は、その下に「ほかのコメントを見る」を表示し、Post Detailへ移動できるようにする。総数はComment導線に表示済みのため、この導線では件数を重複表示しない。

目標の情報順（#44）:

| 順序 | 内容                       | 扱い                                           |
| ---- | -------------------------- | ---------------------------------------------- |
| 1    | 投稿者、イベント・見出し   | 投稿者名は維持し、分類を通常色で表示           |
| 2    | 写真・動画                 | カード幅を使う                                 |
| 3    | ひとこと                   | 存在する場合のみ本文として表示                 |
| 4    | コメント導線、日付、見た人 | コメントを見つけやすくし、日付・人数は補助情報 |
| 5    | 最新コメント、残りへの導線 | 同じ面で自然に続ける                           |

## Media Grid

1枚:

- 大きく1枚

2枚:

- 2分割

3枚:

- 大1 + 小2

4枚以上:

- 代表3枚 + `+N`

写真・動画をできるだけ大きく表示。

Post CardのMediaを押すと、Post Detailを経由せず、選択したMediaをMedia Viewerで直接開く。Media Viewerを閉じると、開く前のTimelineまたはEvent Detailへ戻る。投稿者とCaptionの導線はPost Detailを開く。**目標仕様 #47:** 「コメントを書く」は入力欄が見える状態、「コメントN件」「ほかのコメントを見る」はコメント一覧の開始位置を開く。入力欄への意図的な操作以外でキーボードを自動表示しない。

表示済みのthumbnailとpreviewは端末へ最大300件キャッシュする。originalとvideoに加え、アプリ本体のHTML、JavaScript、CSSはService Workerのキャッシュ対象外とし、ログアウト時にメディアキャッシュを削除する。PWA起動中は、HTMLの初期表示からアプリの起動と認証確認が完了するまで、背景透過のアプリアイコンとアプリ名を画面中央へ途切れず表示する。途中でspinnerだけの画面へ切り替えない。ブラウザでの読み込み中はアプリ名と読み込み状態を表示する。現行はService Worker更新時に新版へ切り替えて自動再読み込みする。**目標仕様 #51:** 未保存入力・選択ファイル・送信中の処理がある間は更新による再読込も保留し、保存完了または明示的な破棄後に更新する。

---

# Album

画面名のヘッダーは表示せず、年の前後移動と月タブを一体化して画面上部へ固定する。公開済みの写真・動画をイベント開始日（未設定なら終了日）の年月にまとめる。月・年をまたぐイベントは開始月へまとめ、重複表示しない。イベントなし・期間未設定の場合のみ撮影日時（Media、Post、公開日時、作成日時の順）の日本時間の日付を使う。APIもこの分類日、撮影日時、Media IDの降順でページングし、撮影日時そのものは変更しない。月の代表Mediaを大きく、そのほかを3列gridで表示する。月タブの左端には、その年の全Mediaを3列gridで表示するグリッドアイコンを置く。月タブには件数を表示しない。年月タブは画像ページから作らず、公開済み・アップロード済みMediaの全年月と件数を別途取得して初回から選択可能にする。選択月または年のMediaを直接60件ずつ取得し、末尾400px手前で自動追加読込する。失敗時は既存画像を残して再試行、IntersectionObserver非対応環境のみ手動ボタンを表示する。期間切替で通信を中止し、別期間の遅延レスポンスを混在させない。Viewerから戻ると選択期間の取得済み件数と位置を復元する。月/年切替中は年月タブを維持し本文だけ対応するSkeletonを表示する。選択色はBottom Navigationと同じテーマ別の色を使う。Mediaを押すとMedia Viewerを開き、月表示から開いた場合は同じ月、年グリッドから開いた場合は同じ年の表示中Mediaを、Postをまたいで前後移動できる。閉じるとアルバムへ戻る。ボトムナビではイベントの右に配置する。

---

# Event List

表示順は進行中、予定、日付未定、終了とする。進行中は終了日が近い順、予定は開始日が近い順、日付未定は更新が新しい順、終了は終了日が新しい順に並べる。進行中と予定のカードには状態バッジを表示する。

一覧では進行中と予定を「進行中・これから」、日付未定を「日付未定」、終了を「これまで」に分け、該当するEventがない区切りは表示しない。

イベントカードはスマホ・タブレット・PC共通で画像を背景全面に表示する。16:9を基本とし、長いタイトルや文字拡大では内容に合わせてカードが伸びる。カバー画像がない場合は暗い代替背景を使用する。

ヘッダー右上にはイベント作成ボタンを置かず、絞り込みボタンを表示する。絞り込みはモーダルで行い、タイトル・メモのキーワード、イベント期間との重なり、進行中・予定・日付未定・終了の状態を組み合わせられる。適用中はヘッダーのボタンに条件数を表示する。イベント作成は共通の追加ボタンから行う。

下端の文字周辺だけにdark gradientを重ね、画像の上部から中央は暗く覆わない。

カード下部はDESIGN STUDY 01に揃え、小さな日付、大きな太字のタイトル、枚数と右矢印の順に白文字で重ねる。一覧ではメモを表示せず、詳細で全文を表示する。状態バッジは進行中・予定のみ上部へ表示する。狭い画面や文字拡大時は下段の折り返しを許容し、情報を欠落させない。スケルトンも同じ全面カバーと下部情報の配置を使う。

表示:

- Event title
- start/end date
- 写真数
- 動画数

必要以上のmetadataは表示しない。

---

# Event Detail

Eventだけに絞ったTimeline。

上部:

- 大きなCover
- Event title
- dates
- 写真数
- 動画数
- Post数

Coverの文字には読める濃さの局所グラデーションを使い、投稿面との境界に意図しない細線を残さない。最下部を純黒にすることは必須にしない（#45）。

メモが入力されている場合は、Coverの次に全文を表示する。改行を保ち、長い文も省略しない。

Event Detailを開いたときは、遷移元のスクロール位置を引き継がず画面先頭から表示する。Coverは画面ヘッダーの下へ固定し、投稿一覧を手前の不透明なsurfaceとして配置する。スクロールすると、上端を角丸にした投稿一覧がCoverへ重なり、最終的にCoverを覆う。Cover画像自体は重なっている間も同じ位置を保つ。現行の最大8%拡大・5pxぼかしは、#52で軽さと写真の見やすさを比較して調整する。固定値は必須にしない。端末で動きを減らす設定が有効な場合は拡大とぼかしを行わない。

ヘッダー右上にはイベントを管理できるユーザー向けの編集ボタンだけを表示し、投稿追加ボタンは置かない。投稿追加は共通の追加ボタンから行い、投稿がない場合は空状態の「写真を追加」からこのイベントを指定したまま開始できる。見出しの追加・名前編集・並び替えはEvent EditとPost Editの両方から行える。

Event Detailで共通の追加ボタンから「写真・動画」を選んだ場合も、投稿の有無にかかわらず表示中のEventを投稿作成画面へ引き継ぐ。引き継いだEventは選択欄で確認・変更できる。存在しないEventがURLで指定された場合はEvent選択を解除し、その理由を入力欄付近へ表示する。

Event Editではタイトル、日付、メモ、見出し、カバーの変更を画面内へ一時保持し、保存前にAPIへ反映しない。いずれかが変更された場合だけ、設定画面と同じ「変更を保存」ボタンをBottom Navigationの上へフェード表示する。保存時はすべての変更を一括して反映する。利用者向けの表記には`Media`ではなく「メディア」を使う。

見出しは一つの文字列として扱い、日と地名を別項目に分解しない。長い文字列の自然な折り返しは許容する。
Event DetailではTimelineと同じPost Cardの構造を使い、見出しによるグループ分けを設けない。投稿カードの情報順を共用し、Event titleはCoverと重複するため省略して見出しだけを補助情報として表示する。見出しにはイベント用のカレンダーアイコンを付けず、見出しがない場合は分類行自体を表示しない。

---

# Post Detail

TimelineまたはEvent Detailの投稿を押したら、元画面を保持してPost Detailをモーダルで開く。写真の位置・大きさを起点に短くズームし、データ取得は待たずに開始する。取得中は実画面と同じ配置のSkeletonを表示し、エラー中も閉じられる。スマートフォンでは全画面、PCでは周囲に元画面が見える余白を設ける。通知・直接URLなど元画面がない入口では通常の投稿詳細を表示する。

モーダルは×・Escape・ブラウザの戻るで閉じ、元画面のスクロール位置を維持する。×とEscapeでは元写真が画面内に残っていればそこへ縮み、見えなければフェードする。開いている間は背景を操作できず、キーボードフォーカスはモーダル内へ収める。投稿詳細からビューワへ進む場合は投稿モーダルを閉じ、戻ったときに投稿詳細を再表示する。削除確認・投稿メニューのEscapeは先にその操作だけを閉じる。


投稿者、Event、見出し、Media Grid、反応、撮影日、CaptionはTimelineと同じ視覚構造で表示する。小さな投稿者アイコンと投稿者名を横並びにし、その下の独立行へ通常色で`Event title ＞ 見出し`を表示する。Event titleにはカレンダーアイコンを付け、Eventと見出しのどちらもない場合は分類の代替文言を表示しない。

CaptionとComment一覧は同じsurface上に連続して配置し、背景色や太い帯で別の島に分けない。Comment一覧の開始位置は1pxの区切り線と余白で示す。

投稿内容とComment一覧だけをページ内でスクロールし、コメント投稿フォームは画面下端へ固定する。現在ユーザーのアイコン、コメント入力欄、送信ボタンを横並びにし、モバイルのsafe areaを確保する。キーボード表示中はフォームをキーボード直上に保ち、Comment一覧の末尾が隠れない余白をスクロール領域へ設ける。

表示:

- 見出し
- Event
- date
- uploader
- Media Grid
- caption
- seen by
- comments

コメントには本文、ユーザー名、日時に加えて、ユーザー名の左にプロフィール画像または表示名の先頭文字を使った丸いアイコンを表示する。

ownerとuploaderには投稿詳細ヘッダーの右端に3点メニューを表示し、「編集」「削除」を配置する。編集ではEvent、見出し、Captionの変更に加え、写真・動画を追加・削除・ドラッグ＆ドロップで並び替えできる。並び替えた順序は保存後も維持する。写真・動画は合計30件まで、最低1件を残す。既存Mediaの削除は保存時に反映し、追加Mediaのアップロードに失敗した場合は失敗した項目だけ再試行できる。投稿自体の削除時は確認モーダルを表示し、写真・動画とコメントも削除されて元に戻せないことを明示する。投稿編集画面から見出しを新規作成でき、追加後はその見出しを選択した状態にする。

投稿とイベントの編集完了後は、編集画面を履歴に残さず元の詳細画面へ戻る。詳細画面の戻る操作で編集画面へ逆戻りさせない。

Mediaを押すとMedia Viewer。

---

# Media Viewer

写真・動画が主役。

- 大画面表示
- 前後移動
- `5 / 18`
- thumbnail strip
- 撮影日時
- uploader
- Event / 見出し / Post
- 保存

写真・動画は左右スワイプで前後へ切り替えられる。メディアは指の横移動に追従し、横移動が一定距離に達し、縦移動より明確に大きい場合だけ切り替える。切り替えに満たない場合は中央へ戻す。左右送りボタンとキーボードの左右キーでも、スワイプと同じ方向・速度の切り替えアニメーションを使う。写真は2本指のピンチ操作で等倍から4倍まで拡大・縮小し、拡大中は1本指で表示位置を移動できる。拡大中の操作は写真送りと判定せず、写真の切り替え・Viewerを閉じる操作で倍率と位置を初期化する。動画上のタッチは再生操作を優先してスワイプ・ピンチ判定から除外する。先頭と末尾では抵抗を付け、それ以上移動しないことを示す。

Media Viewerは下端の横一列のサムネイルを除く領域全体をMedia表示に使う。閉じるボタン・現在位置・保存ボタンは上側、Caption・撮影日・投稿者・Event・見出し・前後移動・Comment導線は下側へ、写真の端を暗く覆いすぎない局所的な黒gradientとともにMediaへ重ねる。写真をタップすると上下の操作Overlayを180msのfadeで表示・非表示にし、写真を切り替えても表示状態を維持する。非表示中の操作はfocus・読み上げ対象から外し、下端のサムネイル列は常に表示する。Comment欄はサムネイル列の直上に表示し、入力欄とサムネイルを重ねない。iOSのsafe areaを確保する。動画の再生操作はOverlay切替より優先し、動画では操作Overlayを常時表示する。

写真切り替えではViewerを再マウントせず、ヘッダー・操作・サムネイル列を維持する。同じ投稿は再取得せず、投稿をまたぐ取得待ち・失敗はメディア領域内に表示する。写真本体の読み込みも同領域で状態を示し、失敗した写真は再試行できる。選択を変えた後に古い取得結果で表示を上書きしない。

画像間の移動はブラウザ履歴へ追加しない。閉じる操作では実際の入口（タイムライン・イベント詳細・アルバム・投稿詳細）へ戻る。直接URLを開いて入口がない場合のみ対象Post Detailへ置き換えて戻す。その後の戻る操作でViewerへ逆戻りしない。

既存のViewerコメント導線は投稿のコメント欄を開く。コメントは写真単位ではなく投稿単位。#47ではこの導線と一覧からの導線を共通の遷移契約へ揃える。

**目標仕様 #50:** 動画の再生マークと長さの表示位置を一覧間で統一する。時間が未取得なら0秒とせず省略または取得中とする。読み込み中・再生可能・失敗は対象動画の近くで識別でき、失敗時に再試行できる。動画の自動再生は下記「写真・動画の閲覧（#56〜#61）」の明示操作・通信条件に従う。

外部共有ボタンは置かない。

---

# Post Create

順番:

```text
写真・動画
Event
見出し
ひとこと（Caption、任意）
投稿
```

Event / 見出しはoptional。

見出しはEvent選択時のみ。

見出しを新規作成でき、追加後はその見出しを選択した状態にする。投稿内容の各項目と投稿ボタンは24px、見出し選択と作成操作は同じまとまり内で12pxの縦間隔を設ける。注意文は入力欄と左端を揃える。見出し作成欄を負の余白で選択欄に重ねない。PCでは写真選択と投稿内容の2列構成を維持する。

写真は選択直後にローカルのObject URLで一覧へ表示する。動画も選択直後に準備中のプレースホルダーを追加し、サムネイルの生成完了後に画像へ切り替える。端末内では最大2件ずつ準備し、全件の完了を待たず完了した項目から反映する。動画は先頭のデコード済みフレームからサムネイルを生成し、動画のデコードは常に1件ずつ行う。写真と動画が混在する場合はそれぞれ1件まで並行する。準備中は各項目にspinnerを重ね、失敗した項目にはその場で再試行できる導線を表示する。

写真・動画はファイル選択に加えてドラッグ＆ドロップでも複数追加できる。PC幅ではドロップ領域を大きく表示し、ファイルを重ねている間は枠線と背景を変えて受付状態を示す。選択後はサムネイルとファイル名を確認でき、投稿開始前は個別に除外できる。非対応形式、0バイト、上限超過では対象ファイル名と理由を表示する。

ローカルでの準備中とアップロード中は区別し、進捗をヘッダー直下にsticky表示する。アップロード中に再試行ボタンを表示せず、失敗または中断した後だけ再試行導線を表示する。投稿編集で写真・動画を追加する場合も同じ表示と処理方式を使う。

不要:

```text
撮影日の扱い
写真・動画の撮影日時を...
```

これは内部仕様。

---

# Invite

ownerは、家族のグループLINEへ一度共有できる閲覧者用の共通招待URLを発行する。URLは複数人が利用でき、有効期限は30日。受付はいつでも終了でき、再発行すると以前のURLの受付を終了する。URLの所持だけでは閲覧権限を付与しない。

招待された人はLINE Login後に「閲覧のリクエストをする」を押す。送信後は「閲覧リクエストを送りました」と「管理者が確認すると、写真や動画を見られるようになります。準備ができたらLINEでお知らせします。」を表示する。未承認の間は写真・動画、サムネイル、イベント名などの非公開情報を表示しない。

ownerには個別のLINEへ「○○さんから閲覧リクエストが届きました」と「確認する」ボタンを送る。遷移先のメンバー管理では確認待ちを一覧表示し、複数人を選択してまとめて承認、または承認しない操作ができる。承認後は本人へ「写真や動画を見られるようになりました」と「このごろを開く」ボタンを送る。通知に失敗しても申請・承認状態を失わせず、アプリ上で状態を確認できる。

「家族への参加をお願いしました」「閲覧を申し込む」は使用せず、利用者向けの呼称を「閲覧リクエスト」に統一する。

---

# Event Create / Edit

- title
- start_date
- end_date
- description
- 見出し管理
- Cover変更
- Event削除

開始日と終了日は「期間」の一つの操作面にまとめ、カレンダーで開始→終了を選ぶ。選択した両端は移動する円、間は週ごとに両端を丸めた帯で示す。帯は選択位置から近い週の順に220msで伸縮し、円は260msで位置を連続させる。ホバーでは開始日より前の日も含めて期間を予告する。一日だけ選んだ状態で前の日を押すと、後から選んだ日を開始日、元の日を終了日とし、元の日を捨てない。同日を2回選ぶと日帰りとなる。動きを減らす設定では移動・伸縮を即時に切り替える。年・月はそれぞれプルダウンで直接変更でき、前後の月へも移動できる。過去日も選択できる。泊数や宿泊向けプリセットは設けない。「決定」でフォームへ反映し、閉じる・Escape・外側クリックでは変更を破棄。「クリア」後に決定すると期間を未設定へ戻せる。開始日だけなど既存の片側指定も保持できる。キーボードの矢印は日・週、Home/Endは週の先頭・末尾へ移動する。文字拡大でも日付を省略せず折り返す。保存処理と離脱保護は従来通り。

---

# Cover Select

Event内Mediaをgridで表示。

- 写真
- 動画thumbnail

選択可能。

「自動選択に戻す」を用意。

**目標仕様 #45:** 選択メディアの表示位置を調整できる。ドラッグに加えてキーボード・ボタン等でも調整でき、一覧と詳細のプレビューを確認する。正規化した焦点位置を保存する最小構成を実装時に決める。保存までは既存カバーへ反映しない。既存データは中央位置を既定値とし、自動選択へ戻すと手動の画像と位置指定を解除する。

---

# Settings

最低限:

- 表示名
- LINE通知
- LINE連携状態
- アプリからの通知の受信状態
- メンバー一覧（ownerのみ。role・LINE通知ON/OFF）
- メンバーの管理（ownerのみ。閲覧リクエストの承認、権限設定・削除、家族共通の招待URL発行画面への導線）
- ログアウト

LINE通知は、ベルアイコン、オン・オフの文言、状態に応じた説明を添えたトグルスイッチで切り替える。切り替え時はアイコンとスイッチをアニメーションさせるが、カード自体の背景色と枠線は変えない。「このごろ」のLINEアカウントを友だち追加していない場合は利用不可と明示する。アプリからの通知が受信可能な場合は状態だけを表示し、受信不可の場合だけLINEで「このごろ」アカウントをブロックしていないか確認するよう案内する。LINEでブロックするとWebhook受信後に受信不可・通知OFFへ自動更新し、友だち追加またはブロック解除後は受信可能へ自動更新する。解除時に通知を自動でONへ戻さず、ユーザーが明示的にONへ切り替える。

新着のLINE通知はテキスト形式とし、まとめた投稿の件数とMedia件数に加えて、batch内で最も新しく公開された投稿へのURLを表示する。写真・動画のサムネイルは通知に載せない。通知から開いた投稿でログインが必要な場合は、LINE Login完了後もその投稿へ戻す。

Bottom Navigationの5つ目は全role共通で歯車アイコンの「設定」とする。独立したメンバー画面は設けず、ownerの設定画面へメンバー一覧を統合する。「メンバーの管理」からさらに1階層下へ移動し、メンバーの権限設定・削除と招待URL発行を行う。削除前には、アクセスできなくなる一方で過去の投稿やコメントは残ることを確認画面で伝える。招待時の権限は、権限名と操作説明を含むカード型ラジオボタンから選択する。

設定項目は「プロフィール」「LINE」「表示」「使い方」「メンバー」「その他」の見出しとカードを共通構造として使用し、設定グループ全体を囲う外枠は表示しない。入力欄、選択肢、トグル、グループ内の区切りなど、操作対象や状態の識別に必要な境界は維持する。「使い方」では初回ガイドとホーム画面追加案内を再表示できる。表示名またはLINE通知が保存済みの値から変わった場合だけ、Bottom Navigationの上へ「変更を保存」ボタンをフェード表示する。保存バーは追加ボタンより前面へ表示する。保存後はトーストを表示してボタンを収納する。選択時に即時反映するテーマ変更は保存ボタンの表示条件に含めず、テーマ設定は保存対象の項目より下へ分離する。テーマは比較しやすさを優先して3択カードを維持する。

設定項目の下部に背景透過のアプリアイコン、アプリ名、`package.json`を参照したバージョンを表示する。アプリ内でロゴを表示する場合は背景透過版を使用し、選択中のテーマに合わせて切り替える。ホーム画面へ登録するPWAアイコンはOS向けの背景付き画像を維持する。

# 戻る操作と閲覧文脈（目標仕様 #48）

| 操作                                 | 遷移後の状態                                       |
| ------------------------------------ | -------------------------------------------------- |
| タブを選ぶ                           | 遷移先の先頭。別タブのスクロールを流用しない       |
| 一覧から詳細を新規に開く             | 詳細の先頭。コメント導線のみ目的のコメント位置     |
| 詳細・Viewerを閉じる／ブラウザバック | 実際の入口の位置と条件を復元                       |
| アルバムからViewerを閉じる           | 選んだ年・月／年全体のモード・最後に見た写真の位置 |
| イベント詳細から一覧へ戻る           | 一覧の絞り込みとカード位置                         |
| 編集保存後                           | 詳細へ置き換え、戻る操作で編集画面へ再入場しない   |
| 直接URLから開く                      | 履歴がない場合に限り安全な既定の入口へ戻す         |

ピクセル位置だけでなく項目IDと条件を保持し、遅延読込後も可能な範囲で同じ項目へ復帰する。削除された項目は近傍へ、該当なしは条件を残した空状態へ戻す。表示データが変わっても無限に復元を試さない。端末間同期や長期の履歴保存は行わない。

# 投稿の離脱保護（目標仕様 #51）

- 未保存の入力・ファイル選択がある状態でアプリ内を離れる場合は、続ける／破棄して離れるを選べる。空のフォームや保存成功後には確認しない。
- ブラウザ終了・再読込の警告は環境上の制限があり保証しない。ファイルを再選択する必要がある場合を明確にし、保存していないデータを復元可能と案内しない。
- ローカル準備、送信中、送信済み、投稿の確定、失敗・中断を区別する。ファイルの送信完了だけで投稿完了と表示しない。
- 全体の件数と各項目の状態を対応させる。測定できない準備工程で架空のパーセントを表示しない。
- 再試行は失敗段階から行い、送信済み項目を重複させない。送信中の編集・除外・離脱の扱いを明確にする。
- 自動更新による再読込も同じ保護対象とする。永続的な動画保存やバックグラウンド送信保証は追加しない。

# 空状態・表示状態の共通契約

空状態はviewerへ投稿作成を勧めない。絞り込み結果0件は「投稿なし」と区別し、条件を解除できる。読み込み中・エラーでも閉じる／戻るは操作可能にする。追加読込の失敗は既存内容を残して末尾で再試行する。設定の保存済み・変更中・保存中・失敗を区別し、テーマの即時反映とLINE通知の保存確定を混同させない。

# 写真・動画の閲覧（#56〜#61）

写真と動画が混在する思い出を、自分のペースで気軽に鑑賞する。動画の割合は半数未満を想定し、種類による優先表示は行わない。写真、イベント単位の整理、既存の5タブ、投稿順、投稿単位の既読を維持する。動画の視聴完了と投稿の既読は別の意味とする。

- 一覧は登録順の写真・動画を共通グリッドに静止サムネイルで表示する。動画は再生マークと長さで識別でき、一覧では自動再生しない。
- 映像タップでViewerを開く。音付き自動再生がブラウザに拒否されたら明示的な再生ボタンで回復できる。
- Viewerは写真・動画の縦横の構図をcontainで保ち、常時閉じる、共通の前へ・次へ、サムネイル選択を提供する。初期表示と写真・動画の切替後は、下部サムネイル一覧だけを横スクロールし、選択項目を中央付近（先頭・末尾ではスクロール可能な範囲）に表示する。前後ボタン・スワイプ・矢印キー・サムネイル選択・投稿をまたぐ読込後に共通で適用し、ページ位置やフォーカスは動かさない。移動は手動のみ。連続再生の切替は置かず、動画終了時は現在の項目に留まり、終了を明示する。末尾の次へは無効にする。
- 動画の終わり近くで直後の1件が動画の場合だけ、手動移動に備えて先読みする。写真を飛ばして動画を探さない。通信量に上限を設け、プレイヤーを大量に作らない。メタデータ取得、動画の冒頭準備、実際に先読みが効いたかは区別して検証する。
- 同一端末・ユーザーで再生位置を保持し、ログアウトや権限変更で別人の情報を引き継がない。履歴は件数を制限し、端末間同期は行わない。
- Viewer内で投稿のコメントを読んで書ける。動画単位のコメントと誤認させず、フォームを閉じたら鑑賞へ戻る。入力中は再生を止める。
- 再生用ファイルは任意。PC用の変換コマンドと取り込みで原本に対応付ける。作成・編集の双方で同じ手順を使い、原本は保存用に残す。
- 大きなファイルはR2へ分割して直接送信し、同じ画面での失敗再試行は送信済み部分を利用する。ページ終了後のファイル復元は保証しない。
- 一覧は必要な代表メディアと件数、最新コメントだけを取得する。全メディアと会話は詳細取得とし、部分配列の長さを全件数として表示しない。

性能はAPI時間、再生操作からplayingまで、bufferingの回数と待機時間で比較する。計測に写真・動画本体、署名URL、コメント、氏名を含めない。テスト素材と実機/実回線の測定を区別する。

## ログイン画面の案内

見出しは「日々のひとこまを、」「おすそわけ。」の間で明示的に改行する。説明文は `「この頃、何してるかな。」ふと気になる日々の様子が、招待された家族にだけ、そっと届きます。` とし、途中に明示改行を入れず、画面幅に応じて自然に折り返す。投稿者の思い出を親や家族へ届ける関係を伝え、全メンバーが投稿する印象や「夫婦」という直接的な呼称を避ける。

### 見出しの編集と順序

両編集画面の見出し一覧右上に「編集／完了」ボタンを置き、編集時だけ各項目の左端に並び替えハンドル、右端に削除ボタンを表示する。ハンドルをつかむと長押しを待たずに追従し、周囲の見出しが場所を空ける。離すと仮の順序を更新する。文字入力とハンドル以外からのスクロールを維持し、Escape・操作中断で元に戻す。ハンドルの上下矢印キーでも並び替えできる。削除・並び替えは画面の保存で確定し、「完了」は編集モードを閉じるだけとする。削除した見出しに属する投稿は見出しなしになる。見出しはイベント単位で共有し、名称と順序を変えても既存投稿との関連を維持する。写真・動画の区切りとしては扱わない。投稿編集の追加・編集・並び替えも保存まで画面内に保持し、新規追加後はその見出しを選択する。保存前に画面を離れれば見出し変更は破棄される。イベントを変更するときは未保存の見出し変更の破棄を確認する。投稿と見出しは同じAPIのトランザクションで保存し、失敗時は入力内容を保持する。

### 投稿編集のメディア並び替え（#82）

サムネイル本体を約350ms長押しすると、写真・動画が指またはマウスに追従する。右下のドラッグアイコンは置かない。移動先に合わせて周囲のサムネイルが滑らかに場所を空け、指を離した位置の順序を保存前の編集内容へ反映する。長押し成立前の移動は通常のスクロールとして扱い、画面端では自動スクロールする。Escape・操作の中断では元の順序を維持する。キーボードではサムネイルへフォーカスし、矢印キーで移動できる。削除・再試行ボタンからドラッグを開始せず、保存・動画取り込み中は並び替えを無効にする。

### 投稿・イベント編集の入力例（#93）

投稿作成・投稿編集・イベント編集の見出し入力には「例: 2日目 午前（午後）」を表示する。投稿のひとこととイベント編集のメモには「行った場所、やったことなど」を表示する。プレースホルダーは入力例のみで、既存の値や保存データを変更しない。

# 操作フィードバック（#99）

触れられる要素を操作の種類ごとに揃え、押下の受付・選択状態・処理の完了を区別して伝える。以下は適用対象の棚卸しと要求仕様であり、実機検証済みを意味しない。強度と動きの共通基準は `05_DESIGN_GUIDE.md`「操作フィードバック」に従う。

| 画面・共通部品                             | 操作対象                                                 | フィードバック                                                                                       |
| ------------------------------------------ | -------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| 共通ヘッダー・追加メニュー・ダイアログ     | 戻る、閉じる、追加、保存、編集、削除、再試行             | 有効なボタンの押下中だけ縮小。実行中は既存の無効化・処理表示を維持                                   |
| PC左ナビ・スマホ下ナビ                     | 各画面へのリンク                                         | 押下を示し、移動後は既存の選択アイコン・文字で現在地を表示                                           |
| タイムライン・新着・投稿詳細・イベント詳細 | 投稿見出し、写真・動画、ひとこと、コメント・見た人の導線 | 画像は軽く縮小、文章の導線は色等を変化。非操作の投稿カード全体は反応させない                         |
| イベント一覧・アルバム                     | イベントカード、年月・メディアのリンク                   | 大きいカードはごく軽く縮小。写真の見やすさと画像比率を維持                                           |
| イベント絞り込み・アルバム切替             | 選択肢、タブ、適用・解除                                 | 押下と選択済みの表示を区別                                                                           |
| お知らせ・設定メニュー                     | 遷移する行                                               | 控えめな背景等で押下を示す。閲覧時刻など非操作行には適用しない                                       |
| ビューワー・使い方                         | 前後、閉じる、サムネイル、ページの印                     | ボタン押下と現在位置を表示。既存のスワイプ・画像ズームを維持                                         |
| 投稿追加・編集                             | ファイル選択、除外、再試行、投稿・保存                   | ボタン押下。ドロップ受付は既存の枠・背景。準備中・送信中・失敗を成功と区別                           |
| 写真・動画の並び替え                       | サムネイルの約350ms長押し、矢印キー                      | 成立時に持ち上げ。ドラッグ中は直接追従し、周囲は場所を空ける                                         |
| 見出し編集                                 | 並び替えハンドル、削除、編集・完了                       | ハンドルは従来どおり長押し待ちなしで開始。ドラッグ中のみ持ち上げる。「完了」を保存成功として扱わない |
| 設定・メンバー管理・招待                   | テーマ、LINE通知、権限の選択                             | 印・つまみ・文言が変化。利用不可の項目は反応させない                                                 |
| 入力フォーム                               | テキスト入力、セレクト                                   | 既存の控えめなフォーカス表示を維持。入力欄の伸縮は追加しない                                         |
| 保存・コメント送信など                     | 明示操作による処理成功                                   | 成功確認後に既存の反映・チェック付き完了表示。自動既読やバックグラウンド取得には完了演出を出さない   |

## 押下と中断

指・マウスを押した時点で視覚反応を開始する。スクロールとして動き始めた場合、対象外へ指が移動した場合、`pointercancel`、フォーカス喪失、画面離脱では押下状態を解除し、中断に確定・成功の演出を出さない。長押しが成立する前の移動はスクロールとして扱う。並び替えはEscape・中断で元の順序を維持する。子の削除ボタンを押して親のカードまで縮小させない。

無効・処理中の操作には押せるような反応を付けず、二重送信を誘発しない。キーボードは標準のEnter／Space動作とフォーカスを保ち、押下表示の追加によって独自のクリックを重複発火しない。動画のネイティブ操作・画像ズーム・スワイプはそれぞれの操作を優先する。

## 動きを減らす設定

動きを減らす場合は装飾的な縮小・拡大・移動を停止し、色・選択印・文言によって同じ状態を伝える。

## 確認観点

スマホ／PC、ライト／ダーク、キーボードで上表の操作を確認する。タップからスクロールへ移る場合、長押し成立前後の中断、子ボタン、無効状態、送信失敗で誤った確定反応がないことを確認する。動きを減らす設定でも操作が成立し、写真・動画・レイアウト・遷移速度を損なわないことを確認する。

# 全画面の質感・動きの目標仕様（#111）

「思い出に触れる道具」を全画面へ展開する。下表は実装済みの一覧ではなく、各画面を実装するときに具体化する設計目標。既存の画面本文にある情報・権限・保存・既読・戻る先の契約を維持し、表現の判断はデザインガイドの改訂基準に従う。招待の共有・承認など別Issueで進行する機能は、その機能実装に合わせて適用する。本改訂で新機能を承認済みとは扱わない。

## 入口と移動

| 画面・機能               | 目標表現・操作                                                                                                         |
| ------------------------ | ---------------------------------------------------------------------------------------------------------------------- |
| ログイン                 | 公開用サンプルによる写真の重なりとキャッチコピーで入口を構成。最初の一度だけ位置が整う。認証前に家族の実写真を使わない |
| 初回ガイド・再表示       | 既存6ページの閲覧者向け内容を保ち、各ページに実UIの短い操作実演を添える。閉じる・前後操作を常時提供                    |
| ホーム画面追加の手動案内 | 実際の端末操作を図で示す。自動案内は復活させず、静止状態でも説明が成立する                                             |
| スマホ下ナビ             | 小さな選択印が移動し、アイコンの線・塗りが切り替わる。ラベルの位置は安定させる                                         |
| PC左ナビ                 | 道具棚のような余白。追加枠は厚みのある主操作として見分けられる                                                         |
| 追加メニュー             | 追加ボタン付近からトレイが開き、写真・動画／イベントを選べる。viewerには出さない                                       |
| 共通ヘッダー・戻る       | 1行タイトルと操作領域を保ち、実際の入口と項目位置へ戻る                                                                |

## 見る・たどる

| 画面・機能               | 目標表現・操作                                                                                 |
| ------------------------ | ---------------------------------------------------------------------------------------------- |
| タイムライン             | 写真、日付、ひとことを写真日記として構成。文章と写真を多重のカードで囲わない                   |
| 年月区切り               | 日付のしおりがヘッダー下に残り、次の年月に押し替わる                                           |
| 投稿の写真・動画         | 押した一枚からViewerが開く。非操作の投稿全体は反応させない                                     |
| 新着の入口               | 黄色いしおり、正しい未閲覧投稿数、取得済み代表写真の小さな重なりで中身を予感させる             |
| 新着の順番閲覧           | 投稿単位の進行を区切りで示す。成功した既読記録にだけ対応させ、全メディアの視聴率と誤認させない |
| 新着の完了               | しおりが収まり「新しい投稿はここまでです」。全写真・動画を見終えたとは表現しない               |
| 見た人                   | 両目アイコンを維持。家族アイコンの重なりから名前を開く。閲覧数を競争的に強調しない             |
| イベント一覧             | 16:9の表紙、太字の題名、簡潔な枚数表示。影は付けず、ホバーで表紙が少し浮き、押下で沈む     |
| イベントの開閉           | 同じ表紙と題名が詳細へ広がり、閉じると元のカード位置へ収まる                                   |
| イベント詳細             | 表紙へ本文の面が重なり、見出しと余白で出来事の流れを作る                                       |
| イベント絞り込み         | 条件の印が定着して結果が切り替わる。0件時も条件と解除操作を保つ                                |
| イベントへの投稿追加     | 対象イベントの小さな表紙と名称を入力画面へ引き継ぎ、追加先を明確にする                         |
| アルバム年表示           | 月の代表写真と年月の文字の大小で、写真カレンダーとして一望できる                               |
| アルバム月表示・年月選択 | 選んだ月の写真群へ近づき、年月を目印に残す。選択印が定位置へ収まる                             |
| アルバムから戻る         | 最後に見た写真の位置へ戻り、その一枚を短く示す。条件・年月も復元する                           |
| 投稿詳細                 | 写真とひとことを連続させ、PCでは会話を横に配置する                                             |

## 鑑賞と会話

| 画面・機能           | 目標表現・操作                                                                                           |
| -------------------- | -------------------------------------------------------------------------------------------------------- |
| Viewerの写真切替     | 指に直接追従して収まる。前後ボタンでも方向を示し、ページ再読込を演出に使わない                           |
| 拡大・縮小           | 触った位置を中心に拡大し、戻す動きに連続性を持たせる。構図をcontainで保つ                                |
| 操作の表示・非表示   | 短いフェード。写真切替後も選んだ表示状態を維持し、局所背景で写真の角を覆いすぎない                       |
| サムネイル           | 選択中の一枚が少し持ち上がり、サムネイル列だけが追従する                                                 |
| 動画再生・待機・終了 | 再生操作の位置で受付と待機を示し、再生後は映像に譲る。終了後は同じ動画に留まり連続再生しない             |
| コメントパネル       | スマホは下、PCは横から開く。写真は残りの領域に収める。閉じると同じメディアへ戻り、投稿単位の会話と分かる |
| ダウンロード         | 受付・準備・失敗を操作の近くに示す。ブラウザへの引渡しを端末保存完了としない                             |
| コメント一覧・入力   | 人物アイコンと文章の余白で家族の声を並べる。入力領域は自然に広がり、キーボードと重ならない               |
| コメント送信         | 送信中は入力を保ち、成功後に会話の末尾へ収まる。失敗時はその場で再試行できる                             |
| お知らせ             | サムネイルと人物アイコンで、誰がどの思い出に関わったかを示す。最終閲覧時刻はownerのみに表示              |
| お知らせから移動     | 対象の投稿・コメントへ進み、目的の位置を一度だけ柔らかく示す                                             |

## 作る・編集する

| 画面・機能           | 目標表現・操作                                                                             |
| -------------------- | ------------------------------------------------------------------------------------------ |
| 投稿作成・編集       | 写真を机に広げる構成。PCは写真の作業領域と入力の2列。保存後は更新した投稿へ戻る            |
| ファイル選択         | 選んだサムネイルがトレイに並ぶ。大量選択に長い順次アニメーションを付けない                 |
| PCのD&D              | 受け皿が反応し、ドロップ後に写真が一覧へ収まる                                             |
| 写真・動画の並び替え | 長押し成立後に対象が持ち上がり、周囲が場所を空ける。成立前のスクロールや中断を維持         |
| イベント・見出し選択 | 選択した項目がラベルとして定着する。未保存の編集と保存済みを区別する                       |
| アップロード・処理中 | 各写真と全体件数の状態を対応させる。送信完了と投稿確定を区別し、未知の進捗率を捏造しない   |
| 除外・再試行         | 対象だけを取り除く／処理へ戻す。成功済み項目と入力内容を残す                               |
| 投稿完了             | 投稿の確定成功後、写真群が公開投稿の配置へつながる                                         |
| イベント作成・編集   | 題名・期間・表紙のプレビューでアルバムを仕立てる感覚を作る。保存前の変更は画面内に保持     |
| 期間選択             | 開始から終了までを一続きの期間として見せる。狭い幅で入力を読める配置へ戻す                 |
| 表紙選択・位置調整   | 選択写真がプレビューに入り、枠内で位置を調整。一覧・詳細の見え方と自動選択へ戻す操作を提供 |
| 見出し管理           | 短い紙片のような面。ハンドルから即座につかみ、場所を空けて収まる。「完了」と保存確定を区別 |

## 設定・家族

| 画面・機能               | 目標表現・操作                                                                               |
| ------------------------ | -------------------------------------------------------------------------------------------- |
| 設定全体・プロフィール   | 大きな外枠を復活させず、余白と面で整理。人物アイコンと表示名をゆったり置く                   |
| LINE通知                 | つまみがしなやかに収まり、オン操作でベルが一度だけ揺れる。オン／オフ・利用不可を文字でも示す |
| LINE連携・受信状態       | 実際の接続状態をアイコンと文言で表す。利用不可のスイッチに操作可能な反応を付けない           |
| テーマ                   | ライト・ダーク・端末に合わせるの3枚を小さな画面見本にし、選択と実画面が呼応する              |
| 設定保存                 | 変更時にバーが現れ、成功後に収納。テーマの即時適用とLINE通知の保存確定を混同させない         |
| アプリ情報               | 現行アイコンのテーマと文字・図の質感を統一する                                               |
| ステージング権限切替     | 役割と表示の対応を明示し、簡潔に切り替える。確認用UIを本番へ出さない                         |
| ログアウト               | 認証状態と移動先を確定し、短い切替で入口へ戻る                                               |
| 招待発行・コピー・共有   | 招待状の面にURLと操作をまとめる。コピー成功はその場の印で示し、共有操作と相手への到着を区別  |
| 招待を受け取る           | 招待状の意匠を引き継ぎ「閲覧のリクエストをする」を明示する                                   |
| 送信済み・承認待ち       | 正確な送信状態が定着する。長い装飾ループを使わず、権限付与前に実写真を見せない               |
| LINE通知から承認         | 対象の名前を確認できる位置へ直行。承認成功後にメンバー一覧へ加わる                           |
| メンバー一覧・権限       | 人物中心の整列。役割の印とできることの説明を対応させ、成功後に確定状態を示す                 |
| 招待終了・期限切れ・削除 | 影響と次の操作を明示。削除確認では誰に何が起こるかを落ち着いて読める                         |

## 共通部品と状態

| 状態・部品           | 目標表現・操作                                                                 |
| -------------------- | ------------------------------------------------------------------------------ |
| 主ボタン・通常ボタン | 役割に応じた厚みと押下の沈み。保存済みの印と押下中の変形を区別                 |
| 入力・セレクト       | 静かな単一フォーカス。文字や入力欄全体を伸縮させない                           |
| メニュー・ダイアログ | 起点近くから開き、柔らかな影で重なりを示す。フォーカスと閉じる操作を保つ       |
| 初回読込             | 完成形に近い配置から取得できた内容へ置換。権限にない操作や未知の件数を描かない |
| 追加読込             | 既存内容を維持し、末尾に続きが加わる                                           |
| 空・結果なし         | 小さな写真台紙などの図と短い案内。viewerに投稿を勧めず、絞り込みなら解除できる |
| 保存・削除確認       | 対象と結果を読める配置。成功の動きは確定後に出す                               |
| 失敗・離脱保護       | 入力と成功済み内容を残し、対象の近くに回復操作を置く。装飾で失敗を隠さない     |

## 展開と確認

最初の試作はイベント開閉、写真並び替え、LINE通知切替。鑑賞・作業・設定で世界観を比較し、共通の影・状態・動きへ整理してから各画面へ展開する。静止画で構図、録画または操作可能な試作で連続性とテンポをレビューし、実機確認済みの範囲を記録する。

モーションの縮退・中断・性能はデザインガイドを参照。動きを減らす設定でも、選択印・文言・位置で同じ意味が伝わること。既存の権限、全画面ガイド、5タブ、手動の動画移動、閲覧文脈の復元を維持する。リアクションの膨らむ表現は新機能候補として保留し、導入する場合は別Issueで機能範囲を決める。

### イベントへの適用（#117）

一覧は16:9の表紙と下端の情報を維持し、ホバーで表紙を3px持ち上げ、押下時は少し沈ませる。追加画像の取得を伴う奥の写真の演出と影は採用しない。動きを減らす設定では位置・拡大縮小を省く。

読み込み済みの表紙を押すと、対応ブラウザでは表紙と題名が詳細へ連続して広がる。詳細取得中は一覧で取得済みの表紙だけを保ち、投稿部分はSkeletonとする。戻る操作では保存済みの絞り込み・一覧位置と選択カードのフォーカスを復元する。直リンク・未取得・削除時は通常の読込／エラー表示、動きを減らす設定では遷移アニメーションを省く。追加先イベントを渡す既存導線と権限は維持する。

マウス・タップ後の画面遷移や戻る操作では、復元したフォーカスの枠を表示しない。フォーカス自体は保ち、Tabなどのキーボード操作へ切り替えたときは識別できる枠を表示する。入力欄のフォーカス表示は維持する。

### ビューワへの適用（#119）

通常の横ドラッグは指の移動量に1対1で追従し、先頭・末尾のみ抵抗を付ける。写真送りの前に演出の完了を待たず、選択と取得を開始し、次のメディア面が操作方向から収まる。連続操作・離脱・新しいジェスチャーで前のアニメーションを中断する。倍率・位置は切替時に初期化し、操作Overlayの表示設定は維持する。

上下端全面のgradientを局所的な丸い操作面へ置き換え、写真の角に余白を残す。選択中のサムネイルは4px持ち上がり、列だけが滑らかに追従する。初回位置合わせと動きを減らす設定では即時に移動する。サムネイル列は80px＋safe area、低い横画面では68pxとし、コメント欄の下端も同じ値を共有する。

コメントはスマホで下から、960px以上で右から開閉し、元のメディア領域の寸法・ズーム・再生位置は変えない。パネル表示中は重なる下部操作を無効化する。閉じる／Escapeで元のコメントボタンへフォーカスを戻す。動画の待機・再生拒否・失敗からの回復と手動移動を維持し、ダウンロードはブラウザへの引渡しのみ。端末への保存完了は表示しない。

## 投稿・イベント編集の質感（#121）

投稿作成・編集は、白い写真枠を付けずにサムネイルを余白のあるトレイへ並べる。サムネイルだけを正方形に切り抜き、元ファイルの縦横比を維持する。PCでは写真と入力の2列、スマートフォンでは写真から入力へ続く。投稿編集のSkeletonも同じ配置にする。

投稿作成でも既存の長押し・矢印キーによる並び替えを使う。準備中・送信中・投稿下書き作成後は並び替えを止め、アップロード対象と表示順のずれを防ぐ。追加枠は空のトレイでは広く、選択後は写真と同じ大きさに収める。送信済みと失敗は各写真へ表示し、実測の全体進捗と既存の失敗分だけの再試行を維持する。投稿確定後の詳細への遷移には標準のView Transitionを使う。

イベント作成・編集では入力中の題名と期間をカードとしてプレビューする。編集時は選択中の表紙と表示位置も反映し、写真がない場合は無地の面にする。見出し・表紙の既存操作と保存バーは維持する。作成時も未保存の入力がある場合に離脱を確認する。

## 遷移方針の更新（#131）

ビューワ内の画像送りを除き、自動スライドイン・スライドアウトは使用しない。投稿詳細は写真を起点としたズーム、コメント欄・保存バー・絞り込み・使い方案内・トーストは短いフェードまたはその場の小さな拡縮へ統一する。ビューワを開くときは押した写真の位置からズームし、開いた後の画像切替はスライドで連続させる。写真タップは投稿詳細を挟まず直接ビューワへ進む。ビューワ表示中は元画面を同じレイアウトに重ねて描画しない。写真のスワイプ・拡大移動、並び替え、スクロールなど指へ直接追従する動きは維持する。動きを減らす設定では自動の変形を止める。本方針は以前の各領域のスライド指定に優先する。

## タイムラインと新着の質感（#116）

タイムラインは月ごとのまとまりの中で年月を固定し、次の月へ押し替わる。年月見出しは縦棒などの装飾を使わず、小さな年と大きな月をベースラインで揃え、文字の強弱と余白で区切る。投稿順はイベント期間・撮影日時など既存の基準を維持する。写真からひとことへ続く余白を整え、投稿ごとの登場アニメーションは付けない。

新着入口は正しい未閲覧投稿数と、取得済みの未閲覧投稿に含まれる代表写真一枚を表示する。該当写真が未取得の場合はしおりの記号に戻し、既読写真や架空の写真を使わない。追加の画像を待つ演出は設けない。閲覧記録の成功後にのみ件数と代表写真を更新する。新着の順番閲覧は現在の投稿の記録成功を印で示し、残り件数・記録失敗時の再試行を維持する。全メディアの視聴率を示す進捗バーは使わない。完了は「新しい投稿はここまでです」と表示する。

## アルバムの質感（#118）

年表示は既存の分類月で写真をまとめ、小さな年と大きな月、実件数で写真カレンダーを構成する。月見出しから月表示へ移り、写真は直接Viewerへ進む。未取得の月の写真を捏造せず、既存の追加読み込みで補う。PCの年表示は6列、月表示は代表写真と一覧の2列を維持する。月の選択は下線を外して穏やかな面の変化で示し、長い月一覧は横にスクロールできる。

Viewerで最後に表示したメディアを保存済みの復帰位置と共に記録し、戻ったときはその写真に短い内側の印を表示する。フォーカスは写真へ戻すが、スクロール位置は既存の位置復元に任せる。動きを減らす設定では印をアニメーションさせない。期間・読込済み件数・追加読込失敗時の既存写真保持、空の期間と取得失敗の区別を維持する。

## 設定の質感（#122）

プロフィールは円形の人物アイコンと表示名を並べ、設定グループの外枠は復活させない。LINE通知は利用できる状態でオンに操作したときだけベルが短く反応する。保存前の通知設定には「未保存」を添え、テーマの即時反映と区別する。テーマ3択にはヘッダー・写真・ひとこと・ナビの構成を示す小さな画面見本を置き、システムは端末の配色に従う。選択枠は均一な2pxの実線とし、未選択時も透明な枠で同じ寸法を保つ。アイコンを持たない設定メニューの文章は行全体を使う。

使い方・アプリ情報・ログアウトと権限切替は角丸と余白を揃える。保存処理・失敗時の入力保持・LINEの連携／受信状態・利用不可の操作抑止を維持する。ステージング専用操作は引き続き本番へ表示しない。動きを減らす設定ではベルの演出を止める。


## 会話とお知らせの質感（#120）

お知らせは名前の頭文字を使った円形の人物印に投稿／コメントの小さな種別印を添え、右側に対象写真を置く。コメントの抜粋は2行まで表示し、長い文章は切り詰める。最終閲覧時刻の表示はownerだけに維持する。

投稿詳細のPC横並びを維持し、コメントの区切り線を余白へ置き換える。ビューワでも実際の人物アイコンと文章を並べる。お知らせのコメント導線は対象コメントまで移動し、削除済みの場合は会話欄へ戻す。送信成功後だけ会話の末尾へ移動し、その一件を短い背景色で示す。送信中・失敗時の入力は保持し、背景のページはスクロールさせない。読み込み配置も人物印と写真の大きさを揃え、動きを減らす設定では色の演出を省く。


## 招待と家族管理の質感（#123）

発行側と受取側に同じ封筒の印と淡い招待状の面を使う。未承認の招待状には実写真やイベント情報を置かない。コピーが成功した場合だけボタンを「コピー済み」にし、LINEの共有画面を開いた操作を相手への到着とは扱わない。発行済みURLには実際の有効期限を表示する。

リクエストの選択は均一な枠とチェックで示す。処理中は選択を固定し、サーバーが返した実処理件数を画面内に残す。承認後はリクエストとメンバーの両一覧を再取得し、一部未処理の人を一覧から消さない。結果取得に失敗しても確定済み件数を消さず、最新状態を再読込する導線を置く。既存の本人確認、権限、期限・終了・削除の影響を維持する。初回読込も招待状と人物行の配置に合わせる。


## 共通部品の質感（#124）

主ボタンは内側の薄い陰影で厚みを示し、有効な押下時だけ1px沈む。メニューは起点側から、確認ダイアログは中央で小さく拡縮・フェードする。自動スライドは使わない。入力欄とセレクトの寸法・フォーカス表示は変えず、キーボード操作の目印を保つ。

空状態には小さな写真台紙の図を添え、条件不一致は虫眼鏡、お知らせとメンバーには対応する記号を使う。図は装飾として扱い、実写真や件数を捏造しない。各画面の案内と操作の権限判定を保ち、閲覧者へ投稿を勧めない。エラーは警告の印と実際の理由、再読み込みまたは再ログインを表示する。

Skeletonの完成形に対応する寸法を変えず、明滅を少しゆっくりにする。追加読み込みや再試行で保持している既存内容・入力・成功済み項目はそのまま残す。動きを減らす設定では押下の移動とメニュー・ダイアログの演出を止め、高コントラスト環境では境界を実線で示す。


## 入口とナビの質感（#115）

ログイン画面には公開用の風景サンプル2枚を重ね、タップで手前の写真を少し広げる。画像は同時に読み込み、操作時の追加取得や長い待機演出を設けない。ログイン導線を先に操作でき、家族の非公開写真は使わない。キャッチコピーは既存の文言を保つ。閲覧者向けの使い方は6ページと常時閉じる操作を保ち、最初の図にも同じサンプルを使う。ホーム画面追加案内は手動で開く。

5つのタブの選択は丸い面と短いアイコンの反応で示す。追加メニューはスマホでは追加ボタンの上、PCでは左ナビの横で拡縮・フェードする。ネイティブのdialogで背景操作を抑止し、Tab・Escape・閉じるボタン・外側のクリックに対応する。閉じたら元のボタンへフォーカスを戻す。viewerには追加メニューを出さず、イベント内で追加する文脈を維持する。動きを減らす設定では自動アニメーションを止める。
